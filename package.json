{
  "name": "app-logger",
  "version": "0.0.5",
  "license": "Apache-2.0 OR CC-BY-4.0",
  "description": "A logger for web components. Allows to print messages and save them into the datastore",
  "devDependencies": {
    "polymer-cli": "^0.17.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "private": false,
  "main": [
    "app-logger.html"
  ],
  "author": "The Advanced REST client authors <arc@mulesoft.com>",
  "scripts": {
    "serve": "node_modules/.bin/polymer serve",
    "lint": "node_modules/.bin/polymer lint app-logger.html",
    "test-sauce": "node_modules/.bin/polymer test --plugin sauce --job-name \"app-logger:local-test\"",
    "test": "node_modules/.bin/polymer test",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/advanced-rest-client/app-logger.git"
  },
  "bugs": {
    "url": "https://github.com/advanced-rest-client/app-logger/issues",
    "email": "arc@mulesoft.com"
  }
}
